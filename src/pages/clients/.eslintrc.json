{
  "rules": {
    "@typescript-eslint/no-unused-vars": [
      "error",
      {
        "varsIgnorePattern": "^(motion|AnimatePresence)$"
      }
    ],
    "no-unused-vars": [
      "error",
      {
        "varsIgnorePattern": "^(motion|AnimatePresence)$"
      }
    ]
  }
}
